" Turn on line numbers
set number

" Highlight matching parenthesis
set showmatch

" Reload vim config with shortcut <leader>rr
map rr :source ~/.vimrc

" Insert space chars when tab pressed
set expandtab

" Number of spaces for tab press
set tabstop=2

" Number of spaces for indentation
set shiftwidth=2
